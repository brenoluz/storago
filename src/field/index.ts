import { Text } from "./text";
import { UUID } from "./uuid";
import { Json } from "./json";
import { Many } from "./many";
export { JsonConfig } from "./json";

export const fields = {
  Text: Text,
  UUID: UUID,
  Json: Json,
  Many: Many,
}